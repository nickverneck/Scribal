FROM node:20-alpine
WORKDIR /app

# Dependencies
COPY package.json ./package.json
RUN npm install --no-audit --no-fund

# App source mounted at runtime for hot-reload
EXPOSE 5174
CMD ["sh", "-c", "npm run dev"]

